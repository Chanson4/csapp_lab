寄存器无法存储的局部变量一般不使用push 而是自行更改rsp

在某些情况下，尤其是在函数调用时，栈必须保持 16 字节的对齐

在 x86-64 的调用约定中，前六个整数或指针参数通过寄存器传递。具体顺序如下：
第一个参数：rdi
第二个参数：rsi
第三个参数：rdx
第四个参数：rcx
第五个参数：r8
第六个参数：r9
超过六个参数的情况，额外的参数将通过栈传递

将数组（字符串）赋值给寄存器时，实际是将指向第一个元素的指针传递给寄存器
